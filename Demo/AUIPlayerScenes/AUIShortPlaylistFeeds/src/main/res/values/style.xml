<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="bottom_tab_title_inactive">
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!--没有选中的样式-->
    <style name="bottom_tab_title_active">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="BottomCheckDialog" parent="android:Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null
        </item><!-- 为了避免在有 NavigationBar 的手机上 Dialog 从 NavigationBar 底部上来。去掉 Dialog 的动画，使用 View 的动画。-->
        <!-- To avoid Dialog coming up from the bottom of the NavigationBar on a phone that has a NavigationBar. Remove the Dialog animation and use the View animation. -->
        <item name="android:layout_width">match_parent</item>
    </style>
</resources>